{% form_theme renderForm '@NovaeZFormBuilder/nova_form_builder/form_fields.html.twig' %}

{{ form_start(renderForm) }}
{{ form_row(renderForm.name) }}
{{ form_row(renderForm.maxSubmissions) }}
{{ form_row(renderForm.fields) }}

<div class="mt-3">
    <button class="btn btn-primary" type="submit">{{ 'Save'|trans }}</button>
</div>
{{ form_end(renderForm) }}